#pragma once

namespace ggg_tools {
/**
 * @brief Run the solver benchmark tool
 * @param argc Argument count
 * @param argv Argument values
 * @return Exit code (0 for success)
 */
int run_benchmark_solvers(int argc, char *argv[]);
} // namespace ggg_tools